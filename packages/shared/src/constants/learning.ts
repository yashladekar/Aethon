export const progressStatuses = [
    'not_started',
    'in_progress',
    'completed',
    'mastered',
    'skipped',
] as const;

export const revisionStatuses = ['pending', 'completed', 'skipped'] as const;
